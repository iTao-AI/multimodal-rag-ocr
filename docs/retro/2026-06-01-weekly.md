# Weekly Retro: Multimodal RAG OCR | 2026-05-28 ~ 2026-06-01

> Generated manually (automated /retro failed with 429).

---

## 1. What Shipped

### Feature: Evidence Collection (6/6 complete)
- 5 PDFs uploaded (S1-S5), 761 total chunks across all documents
- 6 evidence types collected to `~/Developer/Career/04_Outputs/project_improvement_plans/rag-ocr/evidence_pack/`:
  - E1 OCR examples (S1, S2)
  - E2 Chunk statistics (5 rows: S1-S5)
  - E3 Retrieval comparison (10 queries, 2 CSV files including weak/no-hit cases)
  - E4 Answer source tracing (3 JSON records)
  - E5 Failure cases (5 categories)
  - E6 Latency report (8 rows)

### Fixes (15 commits)
| # | Fix | Impact |
|---|-----|--------|
| 1 | Replace random vector fallback with 503 exception (#13) | **Data corruption prevention** — no more garbage vectors inserted on Embedding API failure |
| 2 | Replace sync `requests.post` with async `httpx` in retrieve_documents | **Event loop unblocked** — concurrent requests no longer serialized |
| 3 | Prevent API key leakage from `/config/default` endpoint | Security: masked keys returned instead of real values |
| 4 | Remove hardcoded Jina key from test file | Git hygiene |
| 5 | Preserve header insertion order (set → dict.fromkeys) | Correctness: H1/H2/H3 order no longer lost |
| 6 | Replace all `fetch().json()` with `safeFetchJSON` (response.ok guard) | **Frontend crash prevention** — 500 non-JSON no longer causes white screen |
| 7 | Fix fake frontend buttons + add safeFetch utility | UX: buttons actually work or show "not implemented" |
| 8 | Replace hardcoded paths with env variables (#19) | Portability |
| 9 | Resolve chat 401: backend uses real key when frontend sends `"***"` | Chat works after API key masking |
| 10 | TypeScript: fix 80 compilation errors from versioned imports | Build health |
| 11 | Mobile responsive layout + markdown viewer improvements | Mobile UX |
| 12 | stop_all_services.sh includes Milvus Docker container stop | Ops correctness |
| 13 | Upload dialog functional | Document ingestion works |
| 14 | .gitignore expanded (6 new entries) | Prevents tracking output/cache |
| 15 | Header test made deterministic | Test reliability |

### Infrastructure
- Phase 3: Backend reliability (CORS, mutable defaults, Milvus query limit)
- Phase 4: pytest framework, upload progress, structured logging
- Phase 5: Docker unified orchestration + health checks
- Constitution v1.0.0 → v1.1.0 (Principle VI: Frontend Defaults added)
- PRD v1.1 written and approved

### Documentation
- CLAUDE.md, EXECUTION_WORKFLOW.md (v2→v3→v4.1), ROADMAP.md, PRD.md, constitution.md all refreshed
- README.md / README_CN.md stack references corrected (Vue 3 → React 18)

---

## 2. Metrics

| Metric | Value |
|--------|-------|
| **Period** | 2026-05-28 ~ 2026-06-01 (5 days) |
| **Commits** | 33 total (30 AI, 3 human) on main, non-merge |
| **Commit types** | 15 fix, 11 docs, 4 chore, 1 test |
| **Hotspot files** | `kb_chat.py` (5), `milvus_api.py` (4), `UploadDialog.tsx` (3), `.gitignore` (4) |
| **PRs referenced** | #3, #4, #5, #7, #13, #17, #18, #19, #22, #23, #24, #26, #30, #31 |
| **Test files** | 1 (`test_header_recursive.py` + `test_kb_chat_api.py`) |
| **Backend services** | 4/4 healthy (8006, 8001, 8000, 8501) |
| **Frontend QA** | 92/100 (up from 58/100) |

### Commit Time Distribution
- Peak: 20:54 CST (4 commits in final push session)
- Spread: Commits across 00:00-21:00 CST — mostly afternoon/evening work

---

## 3. What Went Well ✅

1. **P0 bugs fixed first**: Random vector fallback (data corruption) and event loop blocking (performance) were addressed within hours of identifying them
2. **Constitution-driven development**: Principle VI (Frontend Defaults) was formalized early, then used to guide all frontend fixes
3. **Evidence collection completed end-to-end**: 5 PDFs → 6 evidence types → Career directory, all in one session
4. **Documentation stayed current**: EXECUTION_WORKFLOW.md evolved v2→v3→v4.1 as lessons were learned, not retroactively
5. **QA discipline**: Each fix was verified against running services, not just static analysis

---

## 4. What Went Wrong ❌

1. **/retro command failed (429)**: API quota exceeded during compact — automated retro couldn't run. Manual fallback used this time.
2. **Test coverage thin**: Only 1 test file exists (`test_header_recursive.py`). PRD mandates pytest for chunker ≥ 80% coverage, integration tests for Milvus upload/search. The Phase 4 pytest infrastructure was set up but most test files haven't been written.
3. **Frontend shipped incomplete UI**: Multiple non-functional buttons shipped before fix #17 caught them. Constitution Principle VI now mandates 6 UI states per action.
4. **API key in Git history**: Cleared from current code (P0-3), but full `git filter-branch` / BFG sweep to remove from history not yet done.
5. **No PR process**: 33 commits directly to main. PRs exist for Phases 3-5 but the pattern is commit-then-PR, not PR-then-merge.

---

## 5. Key Decisions Made

| Decision | Context | Outcome |
|----------|---------|---------|
| `resolve_api_key()` in backend | Frontend sends `"***"` masked key | Backend falls back to `.env` real key |
| `img: () => null` in Markdown | Page screenshots showing as document content | Images suppressed in viewer |
| Dict.fromkeys for header order | Python set doesn't preserve insertion order | Headers now maintain H1→H2→H3 sequence |
| Constitution v1.1.0 | Repeated frontend issues | Formalized Principle VI: Frontend Defaults |
| Mobile: slide-in sidebar | Desktop sidebar unusable on mobile | Hamburger menu + overlay pattern |

---

## 6. Open Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| Git history still contains API keys | High | Run BFG or `git filter-branch` |
| Test coverage < 20% | High | Write chunker unit tests + Milvus integration tests |
| No CI/CD pipeline | Medium | GitHub Actions for auto-test on push |
| Redis unverified in production | Low | Code supports no-Redis degradation |
| V2.0 OCR requires remote GPU | Medium | AutoDL instance configured but not integrated into main flow |

---

## 7. Next Week Priorities

1. **Write missing tests** (PRD TD3): chunker unit tests (H1/H2 boundary, table protection, cross-page bridge, merge logic) + Milvus upload/search integration test
2. **Complete Git history cleanup**: BFG sweep to remove all API keys from git history
3. **Canary verification**: Run evidence pack benchmark against current code to ensure no regression after all fixes

---

*Report generated: 2026-06-01 | Branch: main | 33 commits | 5 days*
